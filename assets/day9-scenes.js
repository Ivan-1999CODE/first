function accepted(t,i){return Number.isInteger(i)&&i>=0&&i<t.options.length&&(t.correct===null||(Array.isArray(t.correct)?t.correct.includes(i):t.correct===i));}
function booking(){const o={};history.forEach((c,i)=>{const t=lessons[scene].turns[i];if(t?.set)o[t.set]=c;});return o;}
function turnText(){const t={...lessons[scene].turns[turn]},o=booking(),s=slots[o.slot]||slots[0],e=entrySlots[o.entry]||entrySlots[0],cancel=o.decision===1;
 if(t.dynamic==='tableConfirm'){t.line=`A table for two tomorrow at ${s.en}, under Alex Chen. Is that right?`;t.zh=`Alex Chen，明天 ${s.zh}，2 人桌，對嗎？`;t.answer=`Yes. A table for two tomorrow at ${s.en}. Thank you.`;t.options=[`確認：明天 · 2 人 · ${s.label} · Alex Chen`,'改成 4 人（不符本次需求）'];t.result=`訂位完成：明天 ${s.label}，2 人，Alex Chen。`;}
 if(t.dynamic==='ticketConfirm'){t.line=`Two adult tickets for tomorrow at ${e.en}. Is that right?`;t.zh=`明天 ${e.zh}的 2 張成人票，對嗎？`;}
 if(t.dynamic==='decision')t.result=o.decision===1?'已取消待購票券；尚未付款，不收費。':'已確認總額 $36，下一步選付款方式。';
 if(t.dynamic==='payment'){t.line=cancel?'No problem. You haven’t been charged.':'Cash or card?';t.zh=cancel?'沒問題，沒有收費。':'付現還是刷卡？';t.answer=cancel?'Thank you. Goodbye.':'Card, please. / Cash, please.';t.options=cancel?['確認取消 · 不付款']:['刷卡 · $36','付現 · $36'];t.result=cancel?'已確認取消，沒有購買票券。':'教學付款完成，接著核對並取票。';}
 if(t.dynamic==='ticketFinish'){t.line=cancel?'Have a nice day.':`Here are your two adult tickets for tomorrow at ${e.en}. Thank you.`;t.zh=cancel?'祝你有愉快的一天。':`這是你明天 ${e.zh}的 2 張成人票。謝謝。`;t.answer=cancel?'Thank you. Goodbye.':`Two adult tickets for tomorrow at ${e.en}. Thank you.`;t.options=cancel?['沒有購票 · 結束']: [`領取：2 張成人票 · 明天 ${e.label}`,`領取：1 張兒童票 · 明天 ${e.label}`];t.result=cancel?'已完成禮貌取消，沒有門票或費用。':`已領票：明天 ${e.label}，2 張成人票，共 $36。`;}
 return t;
}
function art(kind){const paths={table:'<rect x="47" y="45" width="96" height="82" rx="20"/><path d="M27 66v40m136-40v40M73 27h44m-44 120h44"/><circle cx="95" cy="86" r="20"/>',calendar:'<rect x="30" y="35" width="130" height="115" rx="12"/><path d="M30 67h130M62 23v25m65-25v25M56 95h16m25 0h16m-57 26h16m25 0h16"/>',clock:'<circle cx="95" cy="87" r="59"/><path d="M95 47v40l30 19"/>',name:'<rect x="25" y="42" width="140" height="95" rx="10"/><circle cx="65" cy="78" r="13"/><path d="M44 112q21-35 42 0m20-39h36m-36 23h28"/>',ticket:'<path d="M25 45h140v29q-24 13 0 26v29H25v-29q24-13 0-26Z"/><path stroke-dasharray="5 8" d="M120 48v78"/><path d="M48 65h47m-47 45h47"/>',payment:'<rect x="26" y="42" width="138" height="94" rx="12"/><path d="M26 70h138M47 111h30"/>',museum:'<path d="m24 57 71-35 71 35H24Zm8 15h126M40 75v62m37-62v62m36-62v62m37-62v62M25 149h140"/>'};return `<svg class="booking-icon" viewBox="0 0 190 174" fill="none" stroke="currentColor" stroke-width="4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${paths[kind]||paths.ticket}</svg>`;}
function overview(){return `<section class="hero"><div class="hero-copy"><p class="eyebrow">DAY 09 / SAVE YOUR SPOT</p><h1>訂好位置，<br>把精彩<span>留給旅程。</span></h1><p class="lead">一張餐桌，兩張門票。<br>說清楚人數與時間，<br>安心安排想去的地方。</p><p class="fine">第 9 天 · 預訂位置，還有景點買票</p></div><div class="booking-hero"><p class="eyebrow">YOUR DAY, RESERVED.</p><div class="hero-ticket"><span>CITY MUSEUM / ADMISSION</span>${art('museum')}<strong>2 ADULTS</strong><small>TOMORROW · 10:00</small></div><div class="hero-table"><span>OLIVE BISTRO</span><strong>A table for two.</strong><small>晚餐有位，旅程有期待。</small></div></div></section><section class="panel before-panel"><p class="eyebrow">FIRST TRY / 留下起點</p><h2>先試著說，不急著找答案。</h2><p class="task">${taskBefore}</p>${field('before','第一次的訂位說法')}<p class="fine">單字或短句也可以；輸入會自動保存在此瀏覽器。第六部分再比較現在的說法。</p></section><div class="section-head"><h2>兩件事，自己開口完成。</h2><span class="progress-pill">${state.complete.length} / ${lessons.length} 已練習</span></div><div class="journey-grid">${lessons.map((l,i)=>`<section class="journey-card"><p class="eyebrow">0${i+1} / ${l.en} ${state.complete.includes(l.id)?'✓':''}</p><h3>${l.name}</h3><p>${l.desc}</p><button data-scene="${i}">進入情境 →</button></section>`).join('')}</div><div class="actions"><button class="primary" data-page="1">先學隨身句 →</button></div><p class="main-note">餐廳、景點、日期、票價與規則均為教學設定，沒有實際訂位或付款。教材不錄音，也不自動評分。紀錄僅留在同一瀏覽器及同一網址來源；本機檔案的保存依瀏覽器而定。</p>${footer(-1,1)}`;}
function phrasePage(){const mode=`<div class="tabs"><button data-phrase-mode="library" aria-pressed="${phraseMode==='library'}">我想怎麼說 · ${phrases.length} 句</button><button data-phrase-mode="reply" aria-pressed="${phraseMode==='reply'}">對方問，我來答 · ${clerkReplies.length} 組</button></div>`;
 if(phraseMode==='library')return head('WORDS TO TAKE WITH YOU','把安排，說得清楚。','先選需要的句子；聽一次，再換成自己的人數或時間。')+mode+`<div class="phrases">${phrases.map((p,i)=>`<section class="phrase"><span class="phrase-index">${String(i+1).padStart(2,'0')} / ${p.use}</span><p class="quote" lang="en">${p.en}</p><details class="reveal"><summary>看中文意思</summary><p>${p.zh}</p><p class="fine">${p.note}</p></details><div class="actions">${audioButtons(p.en)}</div></section>`).join('')}</div><section class="panel before-panel"><h3>訂位和買票，分清楚。</h3><p><span lang="en">book a table</span> 是餐廳訂位；<span lang="en">buy tickets</span> 是買票。<span lang="en">for two</span> 表示 2 人，<span lang="en">two adult tickets</span> 表示 2 張成人票。</p></section>`+footer(0,2);
 const q=clerkReplies[replyIndex];return head('LISTEN & REPLY','聽懂一個問題，接一句。','直接開口即可；這裡不錄音，也不自動評分。聽不清楚時可展開逐字稿。')+mode+tabs('reply',replyIndex,clerkReplies)+`<div class="grid"><section class="panel"><p class="eyebrow">STAFF / 對方問你 · ${replyIndex+1} / ${clerkReplies.length}</p>${art('name')}<div class="actions">${audioButtons(q.line)}</div>${transcript(q.line,q.zh)}</section><section class="panel"><h2>${q.name}</h2><p class="task">${q.task}</p>${hints(q.words,q.answer)}<hr><h3>換成你的答案</h3><p>${q.change}</p>${hints(q.changeWords,q.changeAnswer)}</section></div><div class="lesson-footer"><button data-reply="${replyIndex-1}" ${replyIndex===0?'disabled':''}>← 上一組</button>${replyIndex<clerkReplies.length-1?`<button class="primary" data-reply="${replyIndex+1}">下一組 →</button>`:'<button class="primary" data-page="2">進入互動流程 →</button>'}</div>`+footer(0,2);
}
function statusCard(){const o=booking(),s=slots[o.slot],e=entrySlots[o.entry],cancel=o.decision===1;return `<div class="reservation-card ${scene===1?'admission-card':''}" aria-live="polite"><p class="eyebrow">${scene===0?'OLIVE BISTRO / 訂位卡':'CITY MUSEUM / 票券'}</p><div class="card-object">${art(scene===0?'table':'ticket')}<strong>${scene===0?(o.party!==undefined?'2 位用餐':'選擇桌位'):(o.tickets!==undefined?'2 張成人票':'選擇票券')}</strong></div><dl><dt>日期</dt><dd>${o.date!==undefined?'明天':'尚未選擇'}</dd><dt>時間</dt><dd>${(s||e)?.label||'待確認'}</dd>${scene===0?`<dt>姓名</dt><dd>${o.name!==undefined?'Alex Chen':'待提供'}</dd>`:`<dt>總價</dt><dd>${o.decision===undefined?'成人每張 $18':cancel?'$0 · 沒有購買':'$18 × 2 ＝ $36'}</dd><dt>付款</dt><dd>${cancel?'不需付款':o.payment===undefined?'尚未付款':o.payment===0?'已刷卡（教學）':'已付現（教學）'}</dd>`}</dl><span class="order-state">${cancel?'已取消 · 沒有門票':o.confirmed!==undefined?(scene===0?'✓ 訂位完成':'✓ 已領取門票'):'尚待確認'}</span></div>`;}
function sceneView(){const t=turnText();return `<section class="booking-stage"><div class="stage-label"><span>${scene===0?'RESERVATIONS':'TICKET OFFICE'}</span><span>③ 操作情境物件</span></div>${statusCard()}<div class="object-grid ${t.options.length===1?'single-object':''}">${t.options.map((v,i)=>`<button class="booking-object" data-choice="${i}" aria-pressed="${choice===i}" ${!heard||!spoken?'disabled':''}>${art(t.kind)}<strong>${v}</strong></button>`).join('')}</div><p class="fine">${heard&&spoken?'用物件確認你剛說的決定。':'先聽或讀，再確認已開口，才可操作。'}</p><button class="primary" data-act="next" ${canNext()?'':'disabled'}>${turn===lessons[scene].turns.length-1?'完成這段練習 ✓':'確認操作，下一步 →'}</button></section>`;}
function practice(){const l=lessons[scene],t=turnText();return head('MAKE IT HAPPEN','一步一步，把行程訂好。','聽或讀 → 自己開口 → 操作物件。勾選是練習確認，不是語音辨識。')+tabs('scene',scene,lessons)+(finished?`<section class="panel finish-panel"><div class="success-mark">✓</div><h2>${l.name}，完成！</h2>${statusCard()}<p class="task">再用自己的英文總結：${scene===0?'訂位姓名、人數、日期和最後時間。':booking().decision===1?'你決定先不買，並禮貌結束。':'票種、張數、入場時間和總價。'}</p><div class="actions"><button data-scene="${scene}">再練一次</button>${scene<lessons.length-1?`<button data-scene="${scene+1}">另一段流程 →</button>`:''}<button class="primary" data-page="3">試試新的情況 →</button></div></section>`:`<p class="context-strip">${l.context}</p><div class="booking-practice"><section class="panel dialogue-panel"><div class="section-head compact-head"><span class="eyebrow">${turn+1} / ${l.turns.length}</span><button data-scene="${scene}">重新開始本段</button></div><div class="step-track">${l.turns.map((_,i)=>`<span class="${i<=turn?'done':''}"></span>`).join('')}</div><h2>① 聽對方說</h2><div class="actions">${audioButtons(t.line)}</div>${transcript(t.line,t.zh)}<label class="checkline"><input id="heard" type="checkbox" ${heard?'checked':''}>我已聽完，或已讀完逐字稿</label><div class="speaking-heading"><h2>② 換你用英文說</h2><span class="speaking-cue"><span aria-hidden="true">↓</span> 看這裡，換你說</span></div><div class="speaking-task"><p class="task">${t.task}</p>${hints(t.hint,t.answer)}</div><label class="checkline"><input id="spoken" type="checkbox" ${spoken?'checked':''} ${!heard?'disabled':''}>我已用自己的聲音回應</label><p class="feedback ${canNext()?'':'retry'}" role="status" ${feedback?'':'hidden'}>${esc(feedback)}</p><div class="actions"><button data-act="back" ${turn===0?'disabled':''}>← 上一步</button><button class="primary" data-act="next" ${canNext()?'':'disabled'}>${turn===l.turns.length-1?'完成這段練習 ✓':'下一步 →'}</button></div></section>${sceneView()}</div>`)+footer(1,3);}
function ownNeeds(){const m=missions[mission];return head('ONE CLEAR REQUEST','遇到變化，也能說清楚。','集中練一項需求。先口說，再寫自己的版本；不必重走完整流程。')+tabs('mission',mission,missions)+`<div class="grid"><section class="panel"><h2>${m.name}</h2><p class="task">${m.task}</p>${hints(m.hint,m.answer)}</section><section class="panel">${field(m.field,'我的說法')}<p class="fine">一起核對：${m.check} 意思清楚即可，不必逐字符合範例。</p></section></div>`+footer(2,4);}
function pairPage(){const a=pairActivities[pair],checks=state.pairChecks[a.id];return head('TOGETHER, IN CONVERSATION','和老師，完成一次預訂。','師生共看同一畫面。老師依學生實際說法接話，做到一項就勾一項。')+tabs('pair',pair,pairActivities)+`<div class="shared-task-layout"><section class="panel shared-task"><p class="eyebrow">YOUR MISSION</p><h2>${a.name}</h2><p class="task">${a.task}</p><div class="start-cue"><strong>① ${a.first}先說</strong><p class="opening-zh">「${a.opening}」</p><p class="fine">請用自己的英文表達，不必逐字翻譯。</p><strong>② ${a.first==='學生'?'老師接話':'學生回答'}</strong><p>${a.start}</p></div><div class="section-head"><h3>這次要完成的事</h3><span id="pairProgress" class="progress-pill" role="status">${checks.filter(Boolean).length} / ${a.goals.length} 項已做到</span></div>${a.goals.map((g,j)=>`<label class="checkline"><input type="checkbox" data-pair-check="${pair},${j}" ${checks[j]?'checked':''}><span>${g}</span></label>`).join('')}<p class="fine">完成目標：談清楚上述資訊與最後決定；可取消勾選再練。</p>${field('pairNote-'+a.id,'記下一句真正用到的英文')}</section><aside class="teacher-edge"><details class="teacher-guide"><summary>老師引導 <span>需要時再打開</span></summary><div class="teacher-content"><p class="fine">展開後雙方都看得到。</p><h3>先確認起點：${a.first}先說</h3><p>${a.opening}</p>${a.first==='老師'?audioButtons(a.guide[0][1]):'<p>先等學生開口，再依內容接話。</p>'}<p class="context-strip">${a.stock}</p><h3>聽到學生……你可以接</h3>${a.guide.map(g=>`<section class="quick-reply"><p class="eyebrow">${g[0]}</p><p class="quote" lang="en"><strong>${g[1]}</strong></p><p>${g[2]}</p>${audioButtons(g[1])}</section>`).join('')}<details class="reveal"><summary>學生卡住了</summary><p>${a.cue}</p><details class="reveal"><summary>完整參考說法</summary><p lang="en">${a.sample}</p>${audioButtons(a.sample)}</details></details><details class="reveal"><summary>帶領小提醒與完整條件</summary><p>${a.stock}</p><p>已說清楚的資訊不用重問；確認時間時沿用學生的選擇。請先給中文或關鍵字提示，再提供完整句。</p></details></div></details></aside></div>`+footer(3,5);}
function review(){const names=['看完整範例','看關鍵字','能獨立完成'];return head('SEE YOUR PROGRESS','同一道題，看看現在的我。','先不看提示口說，再留下現在的版本。')+`<section class="panel"><p class="task">${taskBefore}</p>${hints(beforeWords,beforeAnswer)}</section><div class="grid review-compare"><section class="panel"><h2>第一次的我</h2><p class="saved-answer">${esc(state.before)||'尚無課前紀錄；可回第一部分留下你的第一次說法。'}</p></section><section class="panel"><h2>現在的我</h2>${field('after','現在的回答')}</section></div><p class="fine">一起核對：有說想訂位，以及明天、晚上 7 點、2 人嗎？</p><section class="panel before-panel"><h2>現在的我，能做到哪一步？</h2><p>依這次真正需要的協助選擇；三格不是考試分數。</p>${skills.map((s,i)=>{const r=state.ratings[s.id];return `<div class="ability-row" data-level="${r}"><h3>${s.text}</h3><div class="ability-status"><div class="ability-meter" aria-hidden="true">${[0,1,2].map(j=>`<span class="${j<=r?'filled':''}"></span>`).join('')}</div><span role="status">${r<0?'尚未自評':names[r]+' · '+(r+1)+' / 3 格'}</span></div><div class="ability-options">${names.map((n,j)=>`<button data-rating="${i},${j}" aria-pressed="${r===j}"><strong>${n}</strong><span>${['有完整句子，我能跟著表達。','給我幾個詞，我能自己組句。','不看提示，我也能說清楚。'][j]}</span></button>`).join('')}</div></div>`;}).join('')}</section><section class="panel before-panel"><h2>留下一句具體回饋。</h2><p>老師與學生一起找出：這次做到了什麼，下次要多練習哪一步。</p></section>`+footer(4,6);}
function homework(){return head('YOUR NEXT DAY OUT','換個條件，自己再試一次。','聽懂資訊、重選練習，再收起提示說自己的版本。勾選只表示已練習。')+`<div class="grid">${homeQuizzes.map((q,k)=>`<section class="panel"><h2>${k+1}. ${q.task}</h2><div class="actions">${audioButtons(q.line)}</div>${transcript(q.line,q.zh)}<div class="question-options">${q.options.map((v,i)=>`<button data-home-choice="${k},${i}" aria-pressed="${homeAnswers[k]===i}">${v}</button>`).join('')}</div><p role="status" class="feedback ${homeAnswers[k]===q.correct?'':'retry'}" ${homeAnswers[k]<0?'hidden':''}>${homeAnswers[k]===q.correct?'答對了！'+q.why:'再聽一次，留意人數、金額或時間；也可展開逐字稿再選。'}</p><label class="checkline"><input type="checkbox" data-home-check="${q.id}" ${state.homeChecks[q.id]?'checked':''}>我已練習聽懂這段資訊</label></section>`).join('')}</div><section class="panel before-panel"><h2>3. 自己說兩段開場</h2><p class="task">${homeTask}</p>${field('homeNote','我的出遊說法')}${hints(homeHint,homeAnswer)}<label class="checkline"><input type="checkbox" data-home-check="written" ${state.homeChecks.written?'checked':''}>我已寫下自己的版本</label><label class="checkline"><input type="checkbox" data-home-check="spoken" ${state.homeChecks.spoken?'checked':''}>我已收起提示，大聲說一次</label><p class="feedback" role="status">已練習 ${homeCount()} / ${homeKeys.length} 項${homeCount()===homeKeys.length?' · ✓ 課後挑戰完成！':''}</p><p class="fine">作答與勾選自動保存在此瀏覽器。朗讀不可用時，可展開文字繼續。</p></section>`+footer(5,7);}
